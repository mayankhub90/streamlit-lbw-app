# temporary test app — replace app.py and commit, then wait for redeploy
import streamlit as st
st.title("SMOKE TEST: Hello from Streamlit")
st.write("If you see this, Streamlit is running and serving app.py correctly.")
st.write("Now try the full app or paste the runtime logs if this appears blank.")
