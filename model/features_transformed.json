try:
    names = preprocessor.get_feature_names_out()
except:
    names = [f"f_{i}" for i in range(preprocessor.transform(X_train).shape[1])]

with open("features_transformed.json","w") as f:
    json.dump(list(names), f)
